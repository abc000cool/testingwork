{
  "name": "user-system-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server/index.ts",
    "start": "node --env-file-if-exists=.env src/server/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"src/server/**/*.test.ts\""
  },
  "dependencies": {
    "express": "^5.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
